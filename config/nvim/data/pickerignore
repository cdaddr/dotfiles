# generated by ai
# Global picker-ignore list for Snacks pickers (config.pickerignore).
# Files matching these patterns are hidden from ALL file pickers
# (files, git_files, smart, grep, recent) even when tracked in git.
# A per-project `.pickerignore` at a repo root is unioned with this list.
#
# Syntax (gitignore-ish, matched via LSP globs):
#   *          any run of chars within a path segment
#   **         any number of path segments
#   ?          one char within a segment
#   {a,b}      alternation        [abc] / [a-z]   char class
#   foo/       ignore everything under a `foo` directory (at any depth)
#   /foo       anchor to the base dir (repo root for project files, cwd here)
#   foo        with no slash, matches `foo` at any depth (name rule)
#   !foo       re-include a path a previous line ignored (last match wins)
#   #          comment
#
# Add global auto-gen patterns below, e.g.:
# **/*.min.js
# **/*.min.css

# lockfiles (never pick these, tracked or not)
.lock
*lock.json
*lock.yaml
*lock.yml
*lock.toml
