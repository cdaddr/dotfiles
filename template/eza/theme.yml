# generated by generate.rb
colourful: true

filekinds:
  normal: { foreground: "#{{colors.fg}}" }
  directory: { foreground: "#{{colors.primary}}" }
  symlink: { foreground: "#{{colors.path}}" }
  pipe: { foreground: "#{{colors.grey_dim}}" }
  block_device: { foreground: "#{{colors.error}}" }
  char_device: { foreground: "#{{colors.error}}" }
  socket: { foreground: "#{{colors.bg_bright}}" }
  special: { foreground: "#{{colors.purple}}" }
  executable: { foreground: "#{{colors.green}}" }
  mount_point: { foreground: "#{{colors.accent}}" }

perms:
  user_read: { foreground: "#{{colors.fg}}" }
  user_write: { foreground: "#{{colors.yellow}}" }
  user_execute_file: { foreground: "#{{colors.green}}" }
  user_execute_other: { foreground: "#{{colors.green}}" }
  group_read: { foreground: "#{{colors.text}}" }
  group_write: { foreground: "#{{colors.yellow}}" }
  group_execute: { foreground: "#{{colors.green}}" }
  other_read: { foreground: "#{{colors.grey_dim}}" }
  other_write: { foreground: "#{{colors.yellow}}" }
  other_execute: { foreground: "#{{colors.green}}" }
  special_user_file: { foreground: "#{{colors.purple}}" }
  special_other: { foreground: "#{{colors.bg_bright}}" }
  attribute: { foreground: "#{{colors.grey_dim}}" }

size:
  major: { foreground: "#{{colors.grey_dim}}" }
  minor: { foreground: "#{{colors.path}}" }
  number_byte: { foreground: "#{{colors.fg}}" }
  number_kilo: { foreground: "#{{colors.text}}" }
  number_mega: { foreground: "#{{colors.primary}}" }
  number_giga: { foreground: "#{{colors.purple}}" }
  number_huge: { foreground: "#{{colors.purple}}" }
  unit_byte: { foreground: "#{{colors.grey_dim}}" }
  unit_kilo: { foreground: "#{{colors.primary}}" }
  unit_mega: { foreground: "#{{colors.purple}}" }
  unit_giga: { foreground: "#{{colors.purple}}" }
  unit_huge: { foreground: "#{{colors.accent}}" }

users:
  user_you: { foreground: "#{{colors.fg}}" }
  user_root: { foreground: "#{{colors.error}}" }
  user_other: { foreground: "#{{colors.purple}}" }
  group_yours: { foreground: "#{{colors.text}}" }
  group_other: { foreground: "#{{colors.grey_dim}}" }
  group_root: { foreground: "#{{colors.error}}" }

links:
  normal: { foreground: "#{{colors.path}}" }
  multi_link_file: { foreground: "#{{colors.accent}}" }

git:
  new: { foreground: "#{{colors.green}}" }
  modified: { foreground: "#{{colors.yellow}}" }
  deleted: { foreground: "#{{colors.error}}" }
  renamed: { foreground: "#{{colors.accent}}" }
  typechange: { foreground: "#{{colors.purple}}" }
  ignored: { foreground: "#{{colors.grey_dim}}" }
  conflicted: { foreground: "#{{colors.error}}" }

git_repo:
  branch_main: { foreground: "#{{colors.fg}}" }
  branch_other: { foreground: "#{{colors.purple}}" }
  git_clean: { foreground: "#{{colors.green}}" }
  git_dirty: { foreground: "#{{colors.error}}" }

security_context:
  colon: { foreground: "#{{colors.grey_dim}}" }
  user: { foreground: "#{{colors.text}}" }
  role: { foreground: "#{{colors.purple}}" }
  typ: { foreground: "#{{colors.bg_bright}}" }
  range: { foreground: "#{{colors.purple}}" }

file_type:
  image: { foreground: "#{{colors.yellow}}" }
  video: { foreground: "#{{colors.error}}" }
  music: { foreground: "#{{colors.green}}" }
  lossless: { foreground: "#{{colors.accent}}" }
  crypto: { foreground: "#{{colors.bg_bright}}" }
  document: { foreground: "#{{colors.fg}}" }
  compressed: { foreground: "#{{colors.path}}" }
  temp: { foreground: "#{{colors.error}}" }
  compiled: { foreground: "#{{colors.accent}}" }
  build: { foreground: "#{{colors.bg_bright}}" }
  source: { foreground: "#{{colors.primary}}" }

punctuation: { foreground: "#{{colors.grey_dim}}" }
date: { foreground: "#{{colors.yellow}}" }
inode: { foreground: "#{{colors.grey_dim}}" }
blocks: { foreground: "#{{colors.comment}}" }
header: { foreground: "#{{colors.fg}}" }
octal: { foreground: "#{{colors.accent}}" }
flags: { foreground: "#{{colors.purple}}" }

symlink_path: { foreground: "#{{colors.path}}" }
control_char: { foreground: "#{{colors.accent}}" }
broken_symlink: { foreground: "#{{colors.error}}" }
broken_path_overlay: { foreground: "#{{colors.bg_bright}}" }
